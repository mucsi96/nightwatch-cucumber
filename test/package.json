{
  "name": "nightwatch-cucumber-test",
  "private": true,
  "scripts": {
    "install-nightwatch-cucumber-with-link": "cd .. && npm link && cd test && npm link nightwatch-cucumber",
    "install-nightwatch-cucumber-with-pack": "cd .. && npm pack && cd test && npm install ../nightwatch-cucumber-*.tgz"
  },
  "devDependencies": {
    "chromedriver": "^2.20.0",
    "cucumber": "^0.9.4",
    "nightwatch": "^0.8.6",
    "phantomjs": "^1.9.19",
    "selenium-server": "^2.48.2"
  }
}
